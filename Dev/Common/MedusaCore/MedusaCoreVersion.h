#pragma once
#include "Core/Utility/Version.h"

namespace MedusaCoreVersion
{
	const extern char* AssemblyTitle;
	const extern char* AssemblyDescription;
	const extern char* AssemblyConfiguration;
	const extern char* AssemblyCompany;
	const extern char* AssemblyProduct;
	const extern char* AssemblyCopyright;

	const extern Medusa::Version AssemblyVersion;
	const extern char* AssemblyLastBuildDate;
}